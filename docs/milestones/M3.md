# Milestone 3: Infrastructure Layer

**Status:** 🏗️ **PLANNING**
**Start Date:** 2025-10-29
**Goal:** Screenshot capture + HTML parsing work independently

---

## Goal

Build screenshot capture and HTML parsing systems that provide infrastructure for M4 (WireframeEditor lens). Both systems must work independently with no interdependencies.

**Success Criteria:**
- [ ] Can capture screenshot from iframe via JavaScript hook
- [ ] Screenshot stored in cache and retrievable by routine
- [ ] Can parse HTML and extract semantic structure
- [ ] Caches (DOM, Console, Variable) store/retrieve data correctly
- [ ] Both systems work independently
- [ ] All tests passing, no regressions
- [ ] Config display polished, provider UX improved

---

## Strategy

**Sequential, Bottom-Up Approach** (Following M2's successful pattern):
1. Build in dependency order (foundation → integration)
2. Port proven code where possible (from Flo)
3. Test each component independently
4. Sequential execution: Screenshots → Parsing → UX
5. ~8 sprints, ~350-400 lines each

**Port vs Build:**
- **Port (simplified):** HTMLParser, JavaScriptExtractor (from Flo - simplify for MVP)
- **Port (adapted):** JavaScript screenshot hook (from Flo WireframeScriptHook)
- **New:** Cache GenServers, Screenshot tool, Integration

---

## Component Architecture

### Dependency Tree

```
Level 0: Cache Foundation
├── ScreenshotCache (GenServer) - In-memory image storage
├── DOMStateCache (GenServer) - Parsed DOM structure
├── ConsoleCache (GenServer) - Console logs
└── VariableStateCache (GenServer) - Variable snapshots

Level 1: Core Logic
├── JavaScript Hook (canvas-based screenshot capture)
├── HTMLParser (extract semantic structure from HTML)
└── JavaScriptExtractor (extract scripts, handlers, functions)

Level 2: Integration
├── Screenshot Tool (PubSub: request → JS → cache → retrieve)
├── Parser Integration (HTML → parse → caches)
└── PubSub Channels (coordination)

Level 3: UX Polish
├── Compact config display (next to title)
├── Provider-specific panels (Ollama models, API keys)
└── Enhanced modal (validation, persistence)
```

### Component Responsibilities

**ScreenshotCache** (~150 lines)
- GenServer for in-memory storage
- Base64 PNG images
- Operations: put(id, image), get(id), clear(id), clear_all()
- Supervision tree integration
- TTL/size limits (optional for MVP)

**JavaScript Hook: ScreenshotCapture** (~400 lines total - 2 sprints)
- Mounted: Setup iframe listeners
- Canvas-based capture (html2canvas or manual)
- PubSub: Send screenshot to Elixir
- Debouncing/throttling
- Error handling

**Screenshot Tool** (~100 lines)
- Tool definition in TestLens
- Execute: Sends PubSub request to JS
- Waits for capture completion
- Retrieves from ScreenshotCache
- Returns image in tool result (content blocks format)

**HTMLParser** (~500 lines, simplified from Flo's 38KB)
- Uses Floki for HTML parsing
- Extract semantic structure:
  - Headings (h1-h6)
  - Forms (inputs, buttons, selects)
  - Interactive elements (links, buttons)
  - Metadata (ids, classes, data-attributes)
- Basic error handling
- Returns structured map

**JavaScriptExtractor** (~200 lines)
- Extract inline <script> blocks
- Extract event handlers (onclick, etc)
- List defined functions
- Returns: `%{scripts: [...], handlers: [...], functions: [...]}`

**DOMStateCache** (~150 lines)
- GenServer storing parsed DOM
- Operations: put(routine_id, dom), get(routine_id)
- Cleared on routine completion

**ConsoleCache** (~150 lines)
- GenServer storing console logs
- Operations: append(routine_id, log), get(routine_id)
- Cleared on routine completion

**VariableStateCache** (~150 lines)
- GenServer storing variable snapshots
- Operations: put(routine_id, vars), get(routine_id)
- Cleared on routine completion

---

## Sprint Breakdown

### Sprint 1: Screenshot Foundation (~350 lines)
**Branch:** `feature/m3-1-screenshot-foundation`
**Goal:** ScreenshotCache working, hook scaffolding

**Components:**
- Create ScreenshotCache GenServer (~150 lines)
- Add to Application supervision tree
- Create basic JavaScript hook structure (~200 lines)
- Hook registration in app.js

**Test:**
- ScreenshotCache operations (put/get/clear)
- GenServer starts and supervised
- Hook mounts on page

**Commit:** "M3 Sprint 1: Screenshot foundation - cache and hook scaffold"

---

### Sprint 2: Screenshot Capture (~400 lines)
**Branch:** `feature/m3-2-screenshot-capture`
**Goal:** Full screenshot capture and PubSub communication

**Components:**
- Complete JavaScript capture logic (~300 lines)
  - Canvas-based screenshot
  - Iframe handling
  - Base64 encoding
- PubSub integration (~100 lines)
  - JS → Elixir channel
  - Event handling in LiveView
  - Store in ScreenshotCache

**Test:**
- Can capture screenshot from test page
- Screenshot sent via PubSub
- Screenshot stored in cache
- Screenshot retrievable

**Commit:** "M3 Sprint 2: Screenshot capture - full JS capture + PubSub"

---

### Sprint 3: Screenshot Tool (~200 lines)
**Branch:** `feature/m3-3-screenshot-tool`
**Goal:** AI can request screenshots via tool

**Components:**
- Add screenshot tool to TestLens (~100 lines)
  - Tool definition
  - Execute: send PubSub request
  - Wait for capture
  - Retrieve from cache
  - Return image content blocks
- Integration test (~100 lines)
  - Full workflow test
  - Mock JS capture (for headless tests)

**Test:**
- Tool call → JS receives request
- JS captures → cache stores
- Tool retrieves → returns image
- Image appears in tool result

**Commit:** "M3 Sprint 3: Screenshot tool - AI can request screenshots"

---

### Sprint 4: HTML Parser (~500 lines)
**Branch:** `feature/m3-4-html-parser`
**Goal:** Parse HTML and extract semantic structure

**Components:**
- Create HTMLParser module (~400 lines)
  - Use Floki for parsing
  - Extract semantic elements
  - Extract metadata
  - Error handling
- Tests (~100 lines)
  - Parse sample HTML
  - Extract all element types
  - Handle malformed HTML

**Test:**
- Parses valid HTML correctly
- Extracts headings, forms, links, buttons
- Extracts ids, classes, attributes
- Handles errors gracefully

**Commit:** "M3 Sprint 4: HTML parser - semantic structure extraction"

---

### Sprint 5: JavaScript Extractor (~200 lines)
**Branch:** `feature/m3-5-javascript-extractor`
**Goal:** Extract JavaScript from HTML

**Components:**
- Create JavaScriptExtractor module (~150 lines)
  - Extract inline scripts
  - Extract event handlers
  - List functions
- Tests (~50 lines)
  - Extract from sample HTML
  - Handle various JS formats

**Test:**
- Extracts <script> blocks
- Extracts onclick, onload, etc
- Lists function definitions
- Handles edge cases

**Commit:** "M3 Sprint 5: JavaScript extractor - extract scripts and handlers"

---

### Sprint 6: Cache GenServers (~500 lines)
**Branch:** `feature/m3-6-cache-genservers`
**Goal:** All cache GenServers working

**Components:**
- DOMStateCache GenServer (~150 lines + ~50 tests)
- ConsoleCache GenServer (~150 lines + ~50 tests)
- VariableStateCache GenServer (~150 lines + ~50 tests)
- Application supervision (~50 lines)

**Test:**
- Each cache: put/get/clear works
- GenServers supervised
- Isolation between routines
- Cleanup on routine completion

**Commit:** "M3 Sprint 6: Cache GenServers - DOM, Console, Variable"

---

### Sprint 7: Parsing Integration (~250 lines)
**Branch:** `feature/m3-7-parsing-integration`
**Goal:** Parser → Caches data flow works

**Components:**
- Wire HTMLParser to DOMStateCache (~50 lines)
- Wire JavaScriptExtractor integration (~50 lines)
- Integration tests (~150 lines)
  - Full parsing workflow
  - Cache population
  - Data retrieval

**Test:**
- Parse HTML → DOM stored in cache
- Extract JS → stored in cache
- Caches accessible by routine
- End-to-end workflow

**Commit:** "M3 Sprint 7: Parsing integration - parser to caches"

---

### Sprint 8: UX Polish & Testing (~600 lines)
**Branch:** `feature/m3-8-ux-polish`
**Goal:** Config display polish + final testing

**Components:**
- Compact config display (~100 lines)
  - Move provider/model next to title
  - 2 lines, small text
  - Always shows actual config
- Provider panels (~300 lines)
  - Ollama: fetch models from API
  - Anthropic: key status
  - OpenAI: key management
- Enhanced modal (~150 lines)
  - Validation before start
  - Persist last config
- Final integration tests (~50 lines)

**Test:**
- Config display accurate and compact
- Provider panels functional
- Modal validates correctly
- All M3 systems work together
- No regressions in M2 features

**Commit:** "M3 Sprint 8: UX polish and final integration"

---

## Integration Points

### PubSub Topics

**Screenshot Communication:**
- `screenshot:request:#{routine_id}` - Elixir → JS: request screenshot
- `screenshot:captured:#{routine_id}` - JS → Elixir: screenshot ready
- Events: `%{routine_id: string, image: base64, timestamp: DateTime}`

**LiveView Hook:**
- Hook name: `ScreenshotCapture`
- Listens for: `phx:screenshot:request`
- Sends: `phx:screenshot:captured` with base64 image

### State Management

**ScreenshotCache:**
- Key: routine_id (string)
- Value: %{image: base64, captured_at: DateTime}
- TTL: Until routine completes

**DOMStateCache:**
- Key: routine_id
- Value: %{elements: [...], metadata: %{}}

**ConsoleCache:**
- Key: routine_id
- Value: [%{type: :log, message: "...", timestamp: ...}]

**VariableStateCache:**
- Key: routine_id
- Value: %{var_name => value}

### Event Flow

**Screenshot Workflow:**
```
AI calls screenshot tool
  ↓
Tool sends PubSub request (screenshot:request:routine_id)
  ↓
JS Hook receives request
  ↓
Hook captures iframe → canvas → base64
  ↓
Hook sends PubSub event (screenshot:captured:routine_id)
  ↓
LiveView receives event
  ↓
LiveView stores in ScreenshotCache
  ↓
Tool retrieves from cache
  ↓
Tool returns image in content blocks
  ↓
AI receives screenshot
```

**Parsing Workflow:**
```
User uploads HTML (future feature)
  ↓
HTMLParser.parse(html)
  ↓
Parser extracts semantic structure
  ↓
Store in DOMStateCache
  ↓
JavaScriptExtractor.extract(html)
  ↓
Store scripts in cache
  ↓
Lens queries caches for context
  ↓
Context includes DOM structure + JS
```

---

## Test Strategy

### Unit Tests

**ScreenshotCache:**
- put/get/clear operations
- Isolation between routines
- Handle invalid routine IDs

**HTMLParser:**
- Parse valid HTML
- Extract all semantic elements
- Handle malformed HTML
- Extract metadata correctly

**JavaScriptExtractor:**
- Extract inline scripts
- Extract event handlers
- List functions
- Handle edge cases

**Cache GenServers:**
- Each cache: CRUD operations
- Supervision tree
- Cleanup on routine end

### Integration Tests

**Screenshot Flow:**
- Mock JS capture
- Full tool workflow
- Cache storage/retrieval
- Error handling

**Parsing Flow:**
- Parse → Cache → Retrieve
- Multiple routines
- Cache isolation

**Full Stack:**
- M2 features still work
- M3 features integrate cleanly
- No performance regressions

### Manual Testing Checklist

**Screenshots:**
1. Open chat with TestLens
2. AI asks for screenshot
3. Tool executes
4. Screenshot captures
5. AI receives image
6. Screenshot visible in conversation

**Parsing:**
1. Upload HTML file (when feature ready)
2. Parser extracts structure
3. Cache stores data
4. Lens queries cache
5. Context includes DOM info

**UX:**
1. Config display shows actual provider/model
2. Config compact and readable
3. Ollama panel shows available models
4. API key panels functional
5. Modal validates before start

---

## Deferred to Later Milestones

**M4: WireframeEditor Lens**
- Full lens using screenshot + parsing
- DOM manipulation tools
- Advanced HTML features

**M5: Polish & Production**
- Screenshot TTL/size limits
- Advanced parser features
- Performance optimization
- Memory management

---

## Progress Tracking

**Completed Sprints:**
- [ ] Sprint 1: Screenshot Foundation
- [ ] Sprint 2: Screenshot Capture
- [ ] Sprint 3: Screenshot Tool
- [ ] Sprint 4: HTML Parser
- [ ] Sprint 5: JavaScript Extractor
- [ ] Sprint 6: Cache GenServers
- [ ] Sprint 7: Parsing Integration
- [ ] Sprint 8: UX Polish & Testing

**Milestone Status:** 🏗️ Planning → Sprint 1 Ready

**Next Steps:**
1. Review and approve plan
2. Create feature branch
3. Start Sprint 1

---

## Total Size Estimate

**~3,000 lines total:**
- Sprint 1: 350 lines (cache + hook scaffold)
- Sprint 2: 400 lines (capture + PubSub)
- Sprint 3: 200 lines (screenshot tool)
- Sprint 4: 500 lines (HTML parser)
- Sprint 5: 200 lines (JS extractor)
- Sprint 6: 500 lines (cache GenServers)
- Sprint 7: 250 lines (parsing integration)
- Sprint 8: 600 lines (UX polish)

Each sprint is independently testable and delivers working components!
