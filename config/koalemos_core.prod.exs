# Core Koalemos engine - production configuration

import Config

# Event logging configuration
config :koalemos,
  # Disable event logging in production (it's for debugging only)
  enable_event_logging: false
